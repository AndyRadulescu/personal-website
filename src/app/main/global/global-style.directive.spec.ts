import { GlobalStyleDirective } from './global-style.directive';

describe('GlobalStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalStyleDirective();
    expect(directive).toBeTruthy();
  });
});
